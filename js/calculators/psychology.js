// Psychology & Behavior Calculators (30)
const psychologyCalculators = [
    { id: 'stress-level', name: 'Stress Level Calculator', icon: '😰', category: 'Psychology', description: 'Assess stress', fields: [{ id: 'work', label: 'Work Stress (1-10)', type: 'number', placeholder: '7' }, { id: 'personal', label: 'Personal Stress (1-10)', type: 'number', placeholder: '5' }, { id: 'health', label: 'Health Concerns (1-10)', type: 'number', placeholder: '4' }], calculate: (v) => { const score = (parseFloat(v.work) + parseFloat(v.personal) + parseFloat(v.health)) / 3 * 10; const level = score < 30 ? 'Low' : score < 60 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '/100', details: `Stress Level: ${level} (${score.toFixed(0)}/100)` }; } },
    { id: 'anxiety-score', name: 'Anxiety Score Calculator', icon: '😟', category: 'Psychology', description: 'GAD assessment', fields: [{ id: 'nervous', label: 'Feeling nervous (0-3)', type: 'number', placeholder: '2' }, { id: 'worry', label: 'Can\'t stop worrying (0-3)', type: 'number', placeholder: '2' }, { id: 'trouble', label: 'Trouble relaxing (0-3)', type: 'number', placeholder: '1' }, { id: 'restless', label: 'Restless (0-3)', type: 'number', placeholder: '1' }], calculate: (v) => { const score = parseFloat(v.nervous) + parseFloat(v.worry) + parseFloat(v.trouble) + parseFloat(v.restless); const level = score < 5 ? 'Minimal' : score < 10 ? 'Mild' : 'Moderate+'; return { result: score + '/12', details: `Anxiety: ${level} (${score}/12)` }; } },
    { id: 'depression-screen', name: 'Depression Screening', icon: '😔', category: 'Psychology', description: 'PHQ assessment', fields: [{ id: 'interest', label: 'Little interest (0-3)', type: 'number', placeholder: '1' }, { id: 'hopeless', label: 'Feeling hopeless (0-3)', type: 'number', placeholder: '1' }, { id: 'sleep', label: 'Sleep issues (0-3)', type: 'number', placeholder: '2' }, { id: 'tired', label: 'Feeling tired (0-3)', type: 'number', placeholder: '2' }], calculate: (v) => { const score = parseFloat(v.interest) + parseFloat(v.hopeless) + parseFloat(v.sleep) + parseFloat(v.tired); const level = score < 4 ? 'Minimal' : score < 7 ? 'Mild' : 'Moderate+'; return { result: score + '/12', details: `${level} - Consult professional if concerned` }; } },
    { id: 'personality-type', name: 'Personality Type', icon: '🧠', category: 'Psychology', description: 'Basic type indicator', fields: [{ id: 'social', label: 'Prefer groups vs alone (1-10)', type: 'number', placeholder: '6' }, { id: 'plan', label: 'Planner vs spontaneous (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const social = parseFloat(v.social), plan = parseFloat(v.plan); const type = (social > 5 ? 'E' : 'I') + (plan > 5 ? 'J' : 'P'); const desc = { EJ: 'Outgoing Planner', EP: 'Outgoing Spontaneous', IJ: 'Reserved Planner', IP: 'Reserved Spontaneous' }; return { result: desc[type], details: `Type: ${desc[type]}` }; } },
    { id: 'eq-score', name: 'Emotional Intelligence', icon: '❤️', category: 'Psychology', description: 'EQ assessment', fields: [{ id: 'aware', label: 'Self-awareness (1-10)', type: 'number', placeholder: '7' }, { id: 'regulate', label: 'Self-regulation (1-10)', type: 'number', placeholder: '6' }, { id: 'empathy', label: 'Empathy (1-10)', type: 'number', placeholder: '8' }, { id: 'social', label: 'Social skills (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.aware) + parseFloat(v.regulate) + parseFloat(v.empathy) + parseFloat(v.social)) / 4 * 10; const level = score < 50 ? 'Developing' : score < 75 ? 'Average' : 'High'; return { result: score.toFixed(0) + '/100', details: `EQ: ${level} (${score.toFixed(0)}/100)` }; } },
    { id: 'focus-span', name: 'Focus Span Calculator', icon: '🎯', category: 'Psychology', description: 'Attention assessment', fields: [{ id: 'task', label: 'Task focus (mins avg)', type: 'number', placeholder: '25' }, { id: 'breaks', label: 'Break frequency (per hr)', type: 'number', placeholder: '4' }], calculate: (v) => { const focus = parseFloat(v.task), breaks = parseFloat(v.breaks); const effectiveTime = focus * (60 / (focus + breaks * 5)); const rating = effectiveTime > 40 ? 'Excellent' : effectiveTime > 25 ? 'Good' : 'Needs work'; return { result: rating, details: `Focus span: ~${focus} mins, Effective work: ${effectiveTime.toFixed(0)} min/hr` }; } },
    { id: 'productivity-score', name: 'Productivity Score', icon: '📈', category: 'Psychology', description: 'Work efficiency', fields: [{ id: 'tasks', label: 'Tasks completed', type: 'number', placeholder: '8' }, { id: 'planned', label: 'Tasks planned', type: 'number', placeholder: '10' }, { id: 'hours', label: 'Hours worked', type: 'number', placeholder: '8' }], calculate: (v) => { const completion = (parseFloat(v.tasks) / parseFloat(v.planned)) * 100; const perHour = parseFloat(v.tasks) / parseFloat(v.hours); return { result: completion.toFixed(0) + '%', details: `Completion: ${completion.toFixed(0)}%, Tasks/hr: ${perHour.toFixed(1)}` }; } },
    { id: 'burnout-risk', name: 'Burnout Risk Calculator', icon: '🔥', category: 'Psychology', description: 'Burnout assessment', fields: [{ id: 'exhaust', label: 'Exhaustion level (1-10)', type: 'number', placeholder: '6' }, { id: 'cynicism', label: 'Cynicism (1-10)', type: 'number', placeholder: '5' }, { id: 'efficacy', label: 'Reduced efficacy (1-10)', type: 'number', placeholder: '4' }], calculate: (v) => { const score = (parseFloat(v.exhaust) + parseFloat(v.cynicism) + parseFloat(v.efficacy)) / 3 * 10; const risk = score < 30 ? 'Low' : score < 60 ? 'Moderate' : 'High'; return { result: risk + ' Risk', details: `Burnout Risk: ${risk} (${score.toFixed(0)}/100)` }; } },
    { id: 'motivation-level', name: 'Motivation Level', icon: '💪', category: 'Psychology', description: 'Motivation check', fields: [{ id: 'energy', label: 'Energy level (1-10)', type: 'number', placeholder: '7' }, { id: 'purpose', label: 'Sense of purpose (1-10)', type: 'number', placeholder: '8' }, { id: 'progress', label: 'Progress feeling (1-10)', type: 'number', placeholder: '6' }], calculate: (v) => { const score = (parseFloat(v.energy) + parseFloat(v.purpose) + parseFloat(v.progress)) / 3 * 10; const level = score < 40 ? 'Low' : score < 70 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '/100', details: `Motivation: ${level}` }; } },
    { id: 'habit-strength', name: 'Habit Strength', icon: '🔄', category: 'Psychology', description: 'Habit analysis', fields: [{ id: 'days', label: 'Consecutive days', type: 'number', placeholder: '21' }, { id: 'triggers', label: 'Clear triggers? (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const days = parseFloat(v.days), triggers = parseFloat(v.triggers); const strength = Math.min(100, days * 2 + triggers * 5); const level = strength < 40 ? 'Forming' : strength < 70 ? 'Developing' : 'Strong'; return { result: level, details: `Habit Strength: ${strength.toFixed(0)}% (${level})` }; } },
    { id: 'sleep-quality', name: 'Sleep Quality Score', icon: '😴', category: 'Psychology', description: 'Sleep assessment', fields: [{ id: 'hours', label: 'Hours slept', type: 'number', placeholder: '7' }, { id: 'wakeups', label: 'Night wakeups', type: 'number', placeholder: '1' }, { id: 'rested', label: 'Feeling rested (1-10)', type: 'number', placeholder: '6' }], calculate: (v) => { const hours = parseFloat(v.hours), wakeups = parseFloat(v.wakeups), rested = parseFloat(v.rested); const score = Math.min(100, hours * 10 - wakeups * 10 + rested * 3); const quality = score < 50 ? 'Poor' : score < 75 ? 'Fair' : 'Good'; return { result: quality, details: `Sleep Quality: ${quality} (${score.toFixed(0)}/100)` }; } },
    { id: 'social-interaction', name: 'Social Interaction Score', icon: '👥', category: 'Psychology', description: 'Social engagement', fields: [{ id: 'convos', label: 'Meaningful convos/day', type: 'number', placeholder: '5' }, { id: 'quality', label: 'Quality rating (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const convos = parseFloat(v.convos), quality = parseFloat(v.quality); const score = Math.min(100, convos * 10 + quality * 5); const level = score < 40 ? 'Low' : score < 70 ? 'Moderate' : 'High'; return { result: level, details: `Social Score: ${score.toFixed(0)}/100` }; } },
    { id: 'cognitive-load', name: 'Cognitive Load', icon: '🧠', category: 'Psychology', description: 'Mental load assessment', fields: [{ id: 'tasks', label: 'Active tasks', type: 'number', placeholder: '7' }, { id: 'decisions', label: 'Decisions/day', type: 'number', placeholder: '50' }], calculate: (v) => { const tasks = parseFloat(v.tasks), decisions = parseFloat(v.decisions); const load = tasks * 5 + decisions * 0.5; const level = load < 30 ? 'Low' : load < 60 ? 'Moderate' : 'High'; return { result: level, details: `Cognitive Load: ${level} (${load.toFixed(0)}/100)` }; } },
    { id: 'learning-style', name: 'Learning Style', icon: '📚', category: 'Psychology', description: 'Identify learning style', fields: [{ id: 'visual', label: 'Learn by seeing (1-10)', type: 'number', placeholder: '8' }, { id: 'auditory', label: 'Learn by hearing (1-10)', type: 'number', placeholder: '5' }, { id: 'kinesthetic', label: 'Learn by doing (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const va = parseFloat(v.visual), au = parseFloat(v.auditory), ki = parseFloat(v.kinesthetic); const max = Math.max(va, au, ki); const style = va === max ? 'Visual' : au === max ? 'Auditory' : 'Kinesthetic'; return { result: style + ' Learner', details: `Primary: ${style} (V:${va} A:${au} K:${ki})` }; } },
    { id: 'memory-retention', name: 'Memory Retention', icon: '💭', category: 'Psychology', description: 'Retention estimate', fields: [{ id: 'initial', label: 'Initial recall (%)', type: 'number', placeholder: '90' }, { id: 'days', label: 'Days since learning', type: 'number', placeholder: '7' }], calculate: (v) => { const initial = parseFloat(v.initial), days = parseFloat(v.days); const retention = initial * Math.exp(-0.1 * days); return { result: retention.toFixed(0) + '%', details: `Retention after ${days} days: ~${retention.toFixed(0)}%` }; } },
    { id: 'mood-tracker', name: 'Mood Score', icon: '😊', category: 'Psychology', description: 'Daily mood average', fields: [{ id: 'morning', label: 'Morning mood (1-10)', type: 'number', placeholder: '6' }, { id: 'afternoon', label: 'Afternoon (1-10)', type: 'number', placeholder: '7' }, { id: 'evening', label: 'Evening (1-10)', type: 'number', placeholder: '8' }], calculate: (v) => { const avg = (parseFloat(v.morning) + parseFloat(v.afternoon) + parseFloat(v.evening)) / 3; const mood = avg < 4 ? 'Low' : avg < 7 ? 'Neutral' : 'Positive'; return { result: mood, details: `Avg Mood: ${avg.toFixed(1)}/10 (${mood})` }; } },
    { id: 'self-esteem', name: 'Self-Esteem Score', icon: '⭐', category: 'Psychology', description: 'Self-esteem assessment', fields: [{ id: 'worth', label: 'Self-worth (1-10)', type: 'number', placeholder: '7' }, { id: 'confidence', label: 'Confidence (1-10)', type: 'number', placeholder: '6' }, { id: 'accept', label: 'Self-acceptance (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.worth) + parseFloat(v.confidence) + parseFloat(v.accept)) / 3 * 10; const level = score < 40 ? 'Low' : score < 70 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '/100', details: `Self-Esteem: ${level}` }; } },
    { id: 'risk-tolerance', name: 'Risk Tolerance', icon: '🎲', category: 'Psychology', description: 'Risk assessment', fields: [{ id: 'comfort', label: 'Comfort with uncertainty (1-10)', type: 'number', placeholder: '6' }, { id: 'recovery', label: 'Loss recovery ability (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.comfort) + parseFloat(v.recovery)) / 2 * 10; const level = score < 40 ? 'Conservative' : score < 70 ? 'Moderate' : 'Aggressive'; return { result: level, details: `Risk Tolerance: ${level} (${score.toFixed(0)}/100)` }; } },
    { id: 'decision-fatigue', name: 'Decision Fatigue', icon: '😵', category: 'Psychology', description: 'Fatigue level', fields: [{ id: 'decisions', label: 'Decisions today', type: 'number', placeholder: '50' }, { id: 'difficulty', label: 'Avg difficulty (1-10)', type: 'number', placeholder: '5' }], calculate: (v) => { const d = parseFloat(v.decisions), diff = parseFloat(v.difficulty); const fatigue = Math.min(100, d * diff / 5); const level = fatigue < 30 ? 'Low' : fatigue < 60 ? 'Moderate' : 'High'; return { result: level, details: `Decision Fatigue: ${level} (${fatigue.toFixed(0)}/100)` }; } },
    { id: 'happiness-index', name: 'Happiness Index', icon: '😄', category: 'Psychology', description: 'Overall happiness', fields: [{ id: 'life', label: 'Life satisfaction (1-10)', type: 'number', placeholder: '7' }, { id: 'positive', label: 'Positive emotions (1-10)', type: 'number', placeholder: '7' }, { id: 'meaning', label: 'Sense of meaning (1-10)', type: 'number', placeholder: '8' }], calculate: (v) => { const score = (parseFloat(v.life) + parseFloat(v.positive) + parseFloat(v.meaning)) / 3 * 10; const level = score < 50 ? 'Needs attention' : score < 75 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '/100', details: `Happiness: ${level}` }; } },
    { id: 'mindfulness', name: 'Mindfulness Score', icon: '🧘', category: 'Psychology', description: 'Present awareness', fields: [{ id: 'present', label: 'Present focus (1-10)', type: 'number', placeholder: '6' }, { id: 'judgement', label: 'Non-judgmental (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.present) + parseFloat(v.judgement)) / 2 * 10; const level = score < 50 ? 'Developing' : score < 75 ? 'Good' : 'Excellent'; return { result: score.toFixed(0) + '/100', details: `Mindfulness: ${level}` }; } },
    { id: 'work-life-balance', name: 'Work-Life Balance', icon: '⚖️', category: 'Psychology', description: 'Balance score', fields: [{ id: 'work', label: 'Work hours/week', type: 'number', placeholder: '45' }, { id: 'personal', label: 'Personal time rating (1-10)', type: 'number', placeholder: '6' }], calculate: (v) => { const work = parseFloat(v.work), personal = parseFloat(v.personal); const balance = Math.max(0, 100 - (work - 40) * 3 + personal * 5); const level = balance < 50 ? 'Imbalanced' : balance < 75 ? 'Moderate' : 'Good'; return { result: level, details: `Balance Score: ${balance.toFixed(0)}/100` }; } },
    { id: 'procrastination', name: 'Procrastination Score', icon: '⏰', category: 'Psychology', description: 'Delay tendency', fields: [{ id: 'delay', label: 'Task delay (1-10)', type: 'number', placeholder: '6' }, { id: 'excuses', label: 'Making excuses (1-10)', type: 'number', placeholder: '5' }], calculate: (v) => { const score = (parseFloat(v.delay) + parseFloat(v.excuses)) / 2 * 10; const level = score < 30 ? 'Low (Good!)' : score < 60 ? 'Moderate' : 'High'; return { result: level, details: `Procrastination: ${score.toFixed(0)}/100` }; } },
    { id: 'mental-age', name: 'Mental Age Estimate', icon: '🧠', category: 'Psychology', description: 'Cognitive age', fields: [{ id: 'actual', label: 'Actual Age', type: 'number', placeholder: '30' }, { id: 'wisdom', label: 'Wisdom level (1-10)', type: 'number', placeholder: '7' }, { id: 'playful', label: 'Playfulness (1-10)', type: 'number', placeholder: '6' }], calculate: (v) => { const actual = parseFloat(v.actual), wisdom = parseFloat(v.wisdom), playful = parseFloat(v.playful); const mental = actual + (wisdom - 5) * 2 - (playful - 5); return { result: mental.toFixed(0) + ' years', details: `Mental Age: ~${mental.toFixed(0)} years` }; } },
    { id: 'stress-recovery', name: 'Stress Recovery Time', icon: '🔄', category: 'Psychology', description: 'Recovery estimate', fields: [{ id: 'intensity', label: 'Stress intensity (1-10)', type: 'number', placeholder: '7' }, { id: 'coping', label: 'Coping skills (1-10)', type: 'number', placeholder: '6' }], calculate: (v) => { const intensity = parseFloat(v.intensity), coping = parseFloat(v.coping); const hours = intensity * 4 / coping; return { result: hours.toFixed(0) + ' hours', details: `Est. Recovery: ${hours.toFixed(0)} hours` }; } },
    { id: 'emotional-stability', name: 'Emotional Stability', icon: '🎭', category: 'Psychology', description: 'Stability score', fields: [{ id: 'swings', label: 'Mood swings (1-10, low=stable)', type: 'number', placeholder: '3' }, { id: 'reactions', label: 'Measured reactions (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const swings = 10 - parseFloat(v.swings), reactions = parseFloat(v.reactions); const score = (swings + reactions) / 2 * 10; const level = score < 50 ? 'Volatile' : score < 75 ? 'Moderate' : 'Stable'; return { result: level, details: `Stability: ${score.toFixed(0)}/100` }; } },
    { id: 'resilience', name: 'Resilience Score', icon: '💪', category: 'Psychology', description: 'Bounce-back ability', fields: [{ id: 'setbacks', label: 'Handle setbacks (1-10)', type: 'number', placeholder: '7' }, { id: 'adapt', label: 'Adaptability (1-10)', type: 'number', placeholder: '8' }, { id: 'optimism', label: 'Optimism (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.setbacks) + parseFloat(v.adapt) + parseFloat(v.optimism)) / 3 * 10; const level = score < 50 ? 'Building' : score < 75 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '/100', details: `Resilience: ${level}` }; } },
    { id: 'wellbeing-index', name: 'Well-Being Index', icon: '🌟', category: 'Psychology', description: 'Overall wellbeing', fields: [{ id: 'physical', label: 'Physical health (1-10)', type: 'number', placeholder: '7' }, { id: 'mental', label: 'Mental health (1-10)', type: 'number', placeholder: '7' }, { id: 'social', label: 'Social connections (1-10)', type: 'number', placeholder: '8' }, { id: 'purpose', label: 'Purpose (1-10)', type: 'number', placeholder: '7' }], calculate: (v) => { const score = (parseFloat(v.physical) + parseFloat(v.mental) + parseFloat(v.social) + parseFloat(v.purpose)) / 4 * 10; const level = score < 50 ? 'Needs attention' : score < 75 ? 'Good' : 'Thriving'; return { result: score.toFixed(0) + '/100', details: `Well-Being: ${level}` }; } },
    { id: 'compatibility', name: 'Personality Compatibility', icon: '💑', category: 'Psychology', description: 'Relationship match', fields: [{ id: 'values', label: 'Shared values (1-10)', type: 'number', placeholder: '8' }, { id: 'comm', label: 'Communication (1-10)', type: 'number', placeholder: '7' }, { id: 'goals', label: 'Common goals (1-10)', type: 'number', placeholder: '8' }], calculate: (v) => { const score = (parseFloat(v.values) + parseFloat(v.comm) + parseFloat(v.goals)) / 3 * 10; const level = score < 50 ? 'Low' : score < 75 ? 'Moderate' : 'High'; return { result: score.toFixed(0) + '% Compatible', details: `Compatibility: ${level}` }; } }
];
if (typeof window !== 'undefined') window.psychologyCalculators = psychologyCalculators;
